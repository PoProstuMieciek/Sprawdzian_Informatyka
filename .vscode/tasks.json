{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "make",
            "type": "shell",
            "group": "build",
            "options": { "cwd": "${workspaceFolder}/build" }
        }
    ]
}
